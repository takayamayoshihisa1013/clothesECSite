// スライド
$(".slider1").slick({
    // autoplay: true,       //自動再生
    // autoplaySpeed: 2000,  //自動再生のスピード
    // speed: 800,           //スライドするスピード
    // dots: true,           //スライド下のドット
    // arrows: true,         //左右の矢印
    // infinite: true,       //永久にループさせる
    // autoplay: true,         //自動再生
    // autoplaySpeed: 3,       //自動再生のスピード
    // speed: 2500,            //追加（スライドスピード
    // slidesToShow:4.0,        //追加（スライドの1スライドごとの表示枚数
    // // cssEase: "linear",      //追加（スライドの動きを等速に
    // pauseOnHover: true,    //追加（ホバーしても止まらないように
    // pauseOnFocus: false,    //追加（フォーカスしても止まらないように

    autoplay: true,       //自動再生
    autoplaySpeed: 2000,  //自動再生のスピード
    speed: 1500,           //スライドするスピード
    dots: true,           //スライド下のドット
    arrows: true,         //左右の矢印
    infinite: true,       //永久にループさせる
    slidesToShow:3,        //追加（スライドの1スライドごとの表示枚数
    pauseOnHover: true,    //追加（ホバーしても止まらないように
    pauseOnFocus: true,    //追加（フォーカスしても止まらないように
    // pauseOnDotsHover: true
})

$(".newsSlider").slick({
    // autoplay: true,       //自動再生
    // autoplaySpeed: 2000,  //自動再生のスピード
    speed: 500,           //スライドするスピード
    dots: true,           //スライド下のドット
    arrows: true,         //左右の矢印
    infinite: true,       //永久にループさせる
    slidesToShow:1,        //追加（スライドの1スライドごとの表示枚数
    pauseOnHover: true,    //追加（ホバーしても止まらないように
    pauseOnFocus: true,    //追加（フォーカスしても止まらないように
})





$(".slider2").slick({
    // autoplay: true,       //自動再生
    // autoplaySpeed: 2000,  //自動再生のスピード
    // speed: 800,           //スライドするスピード
    // dots: true,           //スライド下のドット
    // arrows: true,         //左右の矢印
    // infinite: true,       //永久にループさせる
    autoplay: true,         //自動再生
    autoplaySpeed: 2000,       //自動再生のスピード
    speed: 2500,            //追加（スライドスピード
    slidesToShow:4.0,        //追加（スライドの1スライドごとの表示枚数
    // cssEase: "linear",      //追加（スライドの動きを等速に
    pauseOnHover: true,    //追加（ホバーしても止まらないように
    pauseOnFocus: true,    //追加（フォーカスしても止まらないように
})

$(".slider3").slick({
    autoplay: true,         //自動再生
    autoplaySpeed: 2000,       //自動再生のスピード
    speed: 2500,            //追加（スライドスピード
    slidesToShow:4.0,        //追加（スライドの1スライドごとの表示枚数
    // cssEase: "linear",      //追加（スライドの動きを等速に
    pauseOnHover: true,    //追加（ホバーしても止まらないように
    pauseOnFocus: true,    //追加（フォーカスしても止まらないように
})

$(".slider4").slick({
    autoplay: true,         //自動再生
    autoplaySpeed: 2000,       //自動再生のスピード
    speed: 2500,            //追加（スライドスピード
    slidesToShow:4.0,        //追加（スライドの1スライドごとの表示枚数
    // cssEase: "linear",      //追加（スライドの動きを等速に
    pauseOnHover: true,    //追加（ホバーしても止まらないように
    pauseOnFocus: true,    //追加（フォーカスしても止まらないように
})

$(".slider5").slick({
    // autoplay: true,       //自動再生
    // autoplaySpeed: 2000,  //自動再生のスピード
    // speed: 800,           //スライドするスピード
    // dots: true,           //スライド下のドット
    // arrows: true,         //左右の矢印
    // infinite: true,       //永久にループさせる
    autoplay: true,         //自動再生
    autoplaySpeed: 2000,       //自動再生のスピード
    speed: 2500,            //追加（スライドスピード
    slidesToShow:4.0,        //追加（スライドの1スライドごとの表示枚数
    // cssEase: "linear",      //追加（スライドの動きを等速に
    pauseOnHover: true,    //追加（ホバーしても止まらないように
    pauseOnFocus: true,    //追加（フォーカスしても止まらないように
})


$(".slider6").slick({
    // autoplay: true,       //自動再生
    // autoplaySpeed: 2000,  //自動再生のスピード
    // speed: 800,           //スライドするスピード
    // dots: true,           //スライド下のドット
    // arrows: true,         //左右の矢印
    // infinite: true,       //永久にループさせる
    autoplay: true,         //自動再生
    autoplaySpeed: 2000,       //自動再生のスピード
    speed: 2500,            //追加（スライドスピード
    slidesToShow:4.0,        //追加（スライドの1スライドごとの表示枚数
    // cssEase: "linear",      //追加（スライドの動きを等速に
    pauseOnHover: true,    //追加（ホバーしても止まらないように
    pauseOnFocus: true,    //追加（フォーカスしても止まらないように
})


$(".slider7").slick({
    // autoplay: true,       //自動再生
    // autoplaySpeed: 2000,  //自動再生のスピード
    // speed: 800,           //スライドするスピード
    // dots: true,           //スライド下のドット
    // arrows: true,         //左右の矢印
    // infinite: true,       //永久にループさせる
    autoplay: true,         //自動再生
    autoplaySpeed: 2000,       //自動再生のスピード
    speed: 2500,            //追加（スライドスピード
    slidesToShow:4.0,        //追加（スライドの1スライドごとの表示枚数
    // cssEase: "linear",      //追加（スライドの動きを等速に
    pauseOnHover: true,    //追加（ホバーしても止まらないように
    pauseOnFocus: true,    //追加（フォーカスしても止まらないように
})

$(".slider8").slick({
    // autoplay: true,       //自動再生
    // autoplaySpeed: 2000,  //自動再生のスピード
    // speed: 800,           //スライドするスピード
    // dots: true,           //スライド下のドット
    arrows: true,         //左右の矢印
    infinite: false,       //永久にループさせる
    // autoplay: true,         //自動再生
    // autoplaySpeed: 2000,       //自動再生のスピード
    speed: 1000,            //追加（スライドスピード
    slidesToShow:5.0,        //追加（スライドの1スライドごとの表示枚数
    // cssEase: "linear",      //追加（スライドの動きを等速に
    pauseOnHover: true,    //追加（ホバーしても止まらないように
    pauseOnFocus: true,    //追加（フォーカスしても止まらないように
})

$(".slider9").slick({
    // autoplay: true,       //自動再生
    // autoplaySpeed: 2000,  //自動再生のスピード
    // speed: 800,           //スライドするスピード
    // dots: true,           //スライド下のドット
    arrows: true,         //左右の矢印
    infinite: false,       //永久にループさせる
    // autoplay: true,         //自動再生
    // autoplaySpeed: 2000,       //自動再生のスピード
    speed: 600,            //追加（スライドスピード
    slidesToShow:6.0,        //追加（スライドの1スライドごとの表示枚数
    // cssEase: "linear",      //追加（スライドの動きを等速に
    pauseOnHover: true,    //追加（ホバーしても止まらないように
    pauseOnFocus: true,    //追加（フォーカスしても止まらないように
})

$(".slider10").slick({
    // autoplay: true,       //自動再生
    // autoplaySpeed: 2000,  //自動再生のスピード
    // speed: 800,           //スライドするスピード
    // dots: true,           //スライド下のドット
    arrows: true,         //左右の矢印
    infinite: false,       //永久にループさせる
    // autoplay: true,         //自動再生
    // autoplaySpeed: 2000,       //自動再生のスピード
    speed: 600,            //追加（スライドスピード
    slidesToShow:6.0,        //追加（スライドの1スライドごとの表示枚数
    // cssEase: "linear",      //追加（スライドの動きを等速に
    pauseOnHover: true,    //追加（ホバーしても止まらないように
    pauseOnFocus: true,    //追加（フォーカスしても止まらないように
})

$(".slider11").slick({
    // autoplay: true,       //自動再生
    // autoplaySpeed: 2000,  //自動再生のスピード
    // speed: 800,           //スライドするスピード
    // dots: true,           //スライド下のドット
    arrows: true,         //左右の矢印
    infinite: false,       //永久にループさせる
    // autoplay: true,         //自動再生
    // autoplaySpeed: 2000,       //自動再生のスピード
    speed: 1000,            //追加（スライドスピード
    slidesToShow:5.0,        //追加（スライドの1スライドごとの表示枚数
    // cssEase: "linear",      //追加（スライドの動きを等速に
    pauseOnHover: true,    //追加（ホバーしても止まらないように
    pauseOnFocus: true,    //追加（フォーカスしても止まらないように
})